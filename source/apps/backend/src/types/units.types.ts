export enum Unit {
  Engine = "engine",
  Thermal = "thermal",
}

export enum BothUnit {
  Engine = "engine",
  Thermal = "thermal",
  Both = "both",
}

export enum Action {
  OpenDoors = "opendoors",
  OpenServiceDoors = "openservicedoors",
}
