link-workspace-packages = true
# true on purpose. A lockfile pnpm cannot read fails the install. The file stays unchanged.
frozen-lockfile = true
# false on purpose. A Node mismatch prints and does not stop install.
engine-strict = false
